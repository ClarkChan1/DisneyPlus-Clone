export interface posterAndName {
  poster_path: string;
  id: number;
  contentType: string;
}

export interface contentInfo {
  logo_path: string;
  backdrop_path: string;
  description: string;
  genres: string[];
}
